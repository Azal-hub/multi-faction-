#ifndef CURRENCY_CONVERTER
#define CURRENCY_CONVERTER

// === Currency Converter ===
void runCurrencycurrency(void);

// === Choose Currency Converter ===
int choosecurrency(void);

// === CAD to USA ===
double getCADInput(void);
double giveUSFromoCAD(double input);

// === CAD to EUR ===
double giveEURtoCAD(double input);

// ===  USD to CAN ===
double getUSDInput(void);
double giveCADFromUSD(double input);


#endif // CURRENCY_CONVERTER
